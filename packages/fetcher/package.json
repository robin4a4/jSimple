{
  "name": "@jsimple/fetcher",
  "version": "0.0.1",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": "./dist/index.js"
  },
  "scripts": {
    "dev": "npm run build --watch",
    "build": "tsup src/index.ts --format esm --dts --minify"
  },
  "devDependencies": {
    "tsup": "^6.3.0"
  },
  "dependencies": {
    "@jsimple/core": "workspace:*"
  }
}
